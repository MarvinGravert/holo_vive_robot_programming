using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestLineClass : MonoBehaviour
{
    //// Start is called before the first frame update
    //[SerializeField] private Transform[] points;
    //[SerializeField] private ControlLineRenderer line;

    //private void Start()
    //{
    //    line.SetupLineRenderer(points);
    //}
}
